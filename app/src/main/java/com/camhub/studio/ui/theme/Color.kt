package com.camhub.studio.ui.theme

import androidx.compose.ui.graphics.Color

// Brand Primary
val Primary = Color(0xFF137FEC)
val PrimaryLight = Color(0xFF4DA3FF)
val PrimaryDark = Color(0xFF0A5BB5)

// Backgrounds
val BackgroundDark = Color(0xFF101922)
val BackgroundDarker = Color(0xFF0A1118)

// Surfaces
val SurfaceDark = Color(0xFF1A2736)
val SurfaceLight = Color(0xFF243447)

// Accent Colors
val NeonGreen = Color(0xFF39FF14)
val ElectricRed = Color(0xFFFF3B30)
val AmberYellow = Color(0xFFFFCC00)
val CyanAccent = Color(0xFF00D4FF)

// Glassmorphism
val GlassSurface = Color.White.copy(alpha = 0.03f)
val GlassBorder = Color.White.copy(alpha = 0.1f)

// Tally Indicators
val TallyRed = Color(0xFFFF3B30)
val TallyGreen = Color(0xFF39FF14)

// Text Hierarchy
val TextPrimary = Color.White
val TextSecondary = Color.White.copy(alpha = 0.7f)
val TextTertiary = Color.White.copy(alpha = 0.4f)
val TextMuted = Color.White.copy(alpha = 0.2f)

// LED Status
val LedGreen = Color(0xFF39FF14)
val LedYellow = Color(0xFFFFCC00)
val LedRed = Color(0xFFFF3B30)
val LedOff = Color(0xFF2A3A4D)
