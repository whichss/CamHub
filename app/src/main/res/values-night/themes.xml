<resources xmlns:tools="http://schemas.android.com/tools">
    <style name="Theme.CamHub" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:colorBackground">#FF101922</item>
        <item name="android:windowBackground">#FF101922</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
    </style>
    <style name="Theme.Camera" parent="Theme.CamHub" />
</resources>
